安装：  yum install -y htop
直接输入 htop :
  CPU使用率
  内存使用率
  交换空间使用率
  当前任务数量，多少个任务正在执行
  平均负载
  启动时间
      都是实时更新的
  1 可以通过空格键选择（标记）多个进程，按U取消标记
  2 可以查看一个进程正在使用哪些文件，选中，按 l 键，列出这个进程所有正在使用打开的文件，按Esc键退出
  3 追踪一个进程所有调用过的系统调用，选中，按 s 键
  4 按 q 退出
  5 更多快捷键，通过 man htop 查询
